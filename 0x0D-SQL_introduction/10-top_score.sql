-- Script to list all records of the table second_table of the database hbtn_0c_0
-- Results display both the score and the name, ordered by score
SELECT score, name FROM second_table
ORDER BY score DESC; 
